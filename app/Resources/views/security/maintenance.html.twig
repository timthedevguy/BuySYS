<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Maintenance{% endblock %}</title>

        <link rel="stylesheet" href="{{ asset('/css/bootstrap.min.css') }}" />
        <link rel="stylesheet" href="{{ asset('/css/font-awesome.min.css') }}" />
        <link rel="stylesheet" href="{{ asset('/css/AdminLTE.min.css') }}" />
        <link rel="stylesheet" href="{{ asset('/css/blue.css') }}" />
        <link rel="stylesheet" href="{{ asset('/css/site.css') }}" />
        {% block stylesheets %}{% endblock %}
        <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
        <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
        <!--[if lt IE 9]>
            <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
            <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
        <![endif]-->

        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />

    </head>
    <body class="hold-transition login-page">

        <div class="error-box">
            <div class="login-logo">
              <a href="../../index2.html">
                <b>Am</b>SYS</a>
            </div>

            {{ include('elements/flash.html.twig') }}

            <!-- /.login-logo -->
            <div class="error-box-body" style="width:700px;">
                <div class="error-page">
                    <h2 class="headline text-red"> 503</h2>
                    <div class="error-content">
                        <h3><i class="fa fa-warning text-red"></i> Oops! Under Maintenance.</h3>
                        <p>
                            {{ systemNameShort|raw }} is currently under maintenance.  The system will be back up shortly.  Contact Lorvulk Munba in game for details.
                        </p>
                    </div><!-- /.error-content -->
                </div><!-- /.error-page -->
            </div>
        <!-- /.login-box-body -->
      </div>
      <!-- /.login-box -->
        {% block body %}{% endblock %}

        <script src="{{ asset('/js/jQuery-2.1.4.min.js') }}"></script>
        <script src="{{ asset('/js/bootstrap.min.js') }}"></script>
        <script src="{{ asset('/js/icheck.min.js') }}"></script>
        <script>
            $(function () {
                $('input').iCheck({
                    checkboxClass: 'icheckbox_square-blue',
                    radioClass: 'iradio_square-blue',
                    increaseArea: '20%' // optional
                });
            });
        </script>
    </body>
</html>
